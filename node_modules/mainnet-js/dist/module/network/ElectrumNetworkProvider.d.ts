import { ElectrumClient, ElectrumClientEvents } from "@electrum-cash/network";
import { default as NetworkProvider } from "./NetworkProvider.js";
import { HexHeaderI, TxI, UtxoI, HeaderI } from "../interface.js";
import { Network } from "../interface.js";
import { ElectrumRawTransaction } from "./interface.js";
import { CancelFn } from "../wallet/interface.js";
import { CacheProvider } from "../cache/interface.js";
export default class ElectrumNetworkProvider implements NetworkProvider {
    network: Network;
    private manualConnectionManagement?;
    electrum: ElectrumClient<ElectrumClientEvents>;
    subscriptions: number;
    private subscribedToHeaders;
    private subscriptionMap;
    private _cache;
    get cache(): CacheProvider | undefined;
    constructor(electrum: ElectrumClient<ElectrumClientEvents>, network?: Network, manualConnectionManagement?: boolean | undefined);
    getUtxos(cashaddr: string): Promise<UtxoI[]>;
    getBalance(cashaddr: string): Promise<number>;
    getHeader(height: number, verbose?: boolean): Promise<HeaderI | HexHeaderI>;
    getBlockHeight(): Promise<number>;
    getRawTransaction(txHash: string, verbose?: boolean, loadInputValues?: boolean): Promise<string>;
    getRawTransactionObject(txHash: string, loadInputValues?: boolean): Promise<ElectrumRawTransaction>;
    sendRawTransaction(txHex: string, awaitPropagation?: boolean): Promise<string>;
    getHistory(cashaddr: string, fromHeight?: number, toHeight?: number): Promise<TxI[]>;
    getRelayFee(): Promise<number>;
    watchAddressStatus(cashaddr: string, callback: (status: string) => void): Promise<CancelFn>;
    watchAddress(cashaddr: string, callback: (txHash: string) => void): Promise<CancelFn>;
    watchAddressTransactions(cashaddr: string, callback: (tx: ElectrumRawTransaction) => void): Promise<CancelFn>;
    watchAddressTokenTransactions(cashaddr: string, callback: (tx: ElectrumRawTransaction) => void): Promise<CancelFn>;
    watchBlocks(callback: (header: HexHeaderI) => void, skipCurrentHeight?: boolean): Promise<CancelFn>;
    waitForBlock(height?: number): Promise<HexHeaderI>;
    subscribeToHeaders(callback: (header: HexHeaderI) => void): Promise<CancelFn>;
    subscribeToAddress(cashaddr: string, callback: (data: any) => void): Promise<CancelFn>;
    subscribeToTransaction(txHash: string, callback: (data: any) => void): Promise<CancelFn>;
    private performRequest;
    private trackSubscription;
    private untrackSubscription;
    subscribeRequest(methodName: string, callback: (data: any) => void, ...parameters: (string | number | boolean)[]): Promise<CancelFn>;
    ready(): Promise<boolean | unknown>;
    connect(): Promise<void>;
    disconnect(): Promise<boolean>;
}
//# sourceMappingURL=ElectrumNetworkProvider.d.ts.map